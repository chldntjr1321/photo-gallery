<!-- BEGIN:vite-agent-rules -->
# Vite + React Router v7 + Tailwind CSS v4

- Routing: `react-router` v7 — use `BrowserRouter` + `Routes`/`Route` for basic routing
- Styles: Tailwind CSS v4 via `@tailwindcss/vite` plugin — no config file needed, just `@import "tailwindcss"` in CSS
- Path alias: `@/` maps to `src/`
- Package manager: pnpm
<!-- END:vite-agent-rules -->
